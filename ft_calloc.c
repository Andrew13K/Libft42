/*#include "libft.h"
#include <stdlib.h>

void *ft_calloc(size_t nmemb, size_t size)
{
    size_t *arr=NULL;
    malloc(nmemb * size);
    while (*arr != '\0')
    {
        *arr = 0;
        arr++;
    }
}*/